export interface User {
    Username: string;
    email: string;
    password: string;
}

export interface Project {
    name: string;
    description: string;
}
export interface Task {
    name: string;
    description: string;
}
export interface sorting{
    name:string;
    description: string;
}
